---
export const getUniqueTags = async () => {
  const allRecipes = await Astro.glob('../pages/recipes/*.mdx')
  const uniqueTags = [
    ...new Set(allRecipes.map((recipes) => recipes.frontmatter.tags).flat()),
  ]
  return uniqueTags.sort()
}
---
